This project is depricated and the code will be moved to i_disk

this project contains adig extensions that are specific to java standard edition 
(or in other words will not work in GWT)

This started with the need for a File wrapper for junit stubbing

note the FileAppender classes are for CharacterFiles
if I ever need a set of stubs for binary files I will use the name
BinaryFileAppender, BinaryFileAppenderParams exc